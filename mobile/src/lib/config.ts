// Base URL of the deployed Netlify app. The native app talks to the exact same
// /api/* functions the web app uses — nothing about the backend changed.
export const API_BASE = "https://truebarista-inventory.netlify.app";
